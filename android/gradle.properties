# Specifies the JVM arguments used for the Gradle Daemon.
# This helps to avoid "Out of memory" errors.
org.gradle.jvmargs=-Xmx4096m

# Enables the use of AndroidX libraries, which are the modern standard.
android.useAndroidX=true

# Automatically migrates legacy support libraries to their AndroidX equivalents.
android.enableJetifier=true
