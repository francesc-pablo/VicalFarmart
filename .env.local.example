
# =================================================================
# ======== Firebase Configuration (Client-Side) ===================
# =================================================================
# These keys are safe to be exposed in the browser.
# Get them from your Firebase project settings > General.
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# =================================================================
# ========= Application URL =======================================
# =================================================================
# This is the base URL of your application.
# For local development, it's typically http://localhost:PORT
# Make sure this matches the port you are running the app on.
NEXT_PUBLIC_APP_URL=http://localhost:9002

# =================================================================
# ======== Firebase Admin SDK Configuration (Server-Side) =========
# =================================================================
# These keys are SECRET and must NOT be exposed in the browser.
# Generate a new private key in Firebase project settings > Service accounts.
# NOTE: When you copy the private key, it will have newline characters (\n).
# It's recommended to wrap the entire key in double quotes in your .env.local file.
# e.g., FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n ... \n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY=your_firebase_private_key

# =================================================================
# ======== Google AI (Gemini) API Key (Server-Side) ===============
# =================================================================
# This key is SECRET and must NOT be exposed in the browser.
# Get it from Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key

# =================================================================
# ======== Cloudinary Configuration ===============================
# =================================================================
# Keys for image uploading service.
# Get these from your Cloudinary dashboard settings.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
NEXT_PUBLIC_CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# =================================================================
# ======== Nodemailer Configuration (Server-Side) =================
# =================================================================
# These keys are SECRET and must NOT be exposed in the browser.
# Used for sending transactional emails. Use an app-specific password if using Gmail.
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email_username
EMAIL_PASS=your_email_password
EMAIL_FROM="Vical Farmart <no-reply@yourdomain.com>"
