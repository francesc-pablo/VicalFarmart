
# -----------------------------------------------------------------------------
# Firebase Public Client Configuration
# -----------------------------------------------------------------------------
# These keys are safe to be exposed to the public (e.g., in your browser).
# Get these from your Firebase project settings under "General".
# https://console.firebase.google.com/project/_/settings/general/
# -----------------------------------------------------------------------------
NEXT_PUBLIC_FIREBASE_API_KEY="AIzaSy..."
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="1234567890"
NEXT_PUBLIC_FIREBASE_APP_ID="1:1234567890:web:abcdef1234567890"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-ABCDEFGHIJ"


# -----------------------------------------------------------------------------
# Cloudinary Configuration
# -----------------------------------------------------------------------------
# Your Cloudinary account details for image uploads.
# Find these on your Cloudinary dashboard.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your-cloud-name"
NEXT_PUBLIC_CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"


# -----------------------------------------------------------------------------
# Email (Nodemailer) Service Configuration
# -----------------------------------------------------------------------------
# SMTP details for your email provider (e.g., Gmail, SendGrid, Mailgun).
# -----------------------------------------------------------------------------
EMAIL_HOST="smtp.example.com"
EMAIL_PORT="587"
EMAIL_USER="your-email@example.com"
EMAIL_PASS="your-email-password-or-app-password"
EMAIL_FROM="Vical Farmart <noreply@yourdomain.com>"


# -----------------------------------------------------------------------------
# Firebase Admin SDK Server-Side Configuration (SECRET)
# -----------------------------------------------------------------------------
# This is a SECRET and should NEVER be exposed to the public.
# It's used for server-side administrative tasks.
# 1. Go to Firebase Console > Project Settings > Service Accounts.
# 2. Click "Generate new private key" and download the JSON file.
# 3. Open the JSON file, copy its entire content, and paste it here as a
#    single line, enclosed in double quotes.
# Example: FIREBASE_SERVICE_ACCOUNT_JSON="{...your key content...}"
# -----------------------------------------------------------------------------
FIREBASE_SERVICE_ACCOUNT_JSON=""
