# =================================================================
# Vical Farmart Environment Variables
# =================================================================
#
# Copy this file to .env.local and fill in the values.
# The server needs to be restarted for these changes to take effect.

# -----------------------------------------------------------------
# Firebase Credentials
# Required for core application functionality (database, auth).
# Get these from your Firebase project settings.
# -----------------------------------------------------------------
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# -----------------------------------------------------------------
# Cloudinary Credentials
# Required for image uploads.
# Get these from your Cloudinary account dashboard.
# -----------------------------------------------------------------
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
NEXT_PUBLIC_CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# -----------------------------------------------------------------
# Nodemailer SMTP Credentials
# Required for sending transactional emails (e.g., welcome, orders).
# Use your email provider's SMTP settings (e.g., Gmail, SendGrid).
# -----------------------------------------------------------------
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email_user@example.com
EMAIL_PASS=your_email_password
EMAIL_FROM="Vical Farmart <noreply@yourdomain.com>"
